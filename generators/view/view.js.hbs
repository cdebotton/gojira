'use strict';

module.exports = App.{{#camelize}}{{name}}{{/camelize}}View = App.View.extend({
  model: App.{{#camelize}}{{name}}{{/camelize}}Model,

  template: require('templates/{{name}}'),

  className: '{{name}}',

  events: {

  },

  initialize: function() {
    _.bindAll(this);
  }
});
