'use strict';

module.exports = App.{{#camelize}}{{name}}{{/camelize}}Model = Backbone.RelationalModel.extend({
  relations: [{
    type            : 'Backbone.HasMany',
    relatedModel    : 'App.SomeModel',
    collectionType  : 'App.SomeCollection',
    key             : 'subModels',
    reverseRelation: {
      includeInJSON: 'id'
    }
  }],

  defaults: {

  },

  initialize: function() {

  }
});
