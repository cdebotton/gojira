'use strict';

module.exports = App.{{#camelize}}{{name}}{{/camelize}}Collection = new (Backbone.Collection.extend({
  model: App.{{#camelize}}{{name}}{{/camelize}}Model,

  initialize: function() {
    _.bindAll(this);
  }
}));
