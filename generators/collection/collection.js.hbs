'use strict';

module.exports = App.{{#camelize}}{{name}}{{/camelize}}Collection = Backbone.Collection.extend({
  model: App.{{#camelize}}{{name}}{{/camelize}}Model,

  initialize: function() {

  }
});
