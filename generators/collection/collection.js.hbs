'use strict';

module.exports = App.{{#camelize}}{{pluralName}}{{/camelize}}Collection = Backbone.Collection.extend({
  model: App.{{#camelize}}{{name}}{{/camelize}}Model,

  initialize: function() {

  }
});
